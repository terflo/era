export interface IProduct {
    id: number,
    name: string,
    description: string,
    availableCount: number,
    price: number,
    width: number,
    height: number,
    depth: number,
    imageUrl: string

}